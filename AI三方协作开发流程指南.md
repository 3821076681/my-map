# AI 三方协作开发流程指南

> 本文档记录了通过 **DeepSeek + 豆包 + AI 审核助手** 三方协作开发软件项目的完整流程、角色分工、指令模板和注意事项。

---

## 一、协作模式概述

由四方共同参与项目开发，通过用户作为中转枢纽进行信息传递：

| 角色 | 职责 |
|------|------|
| **用户（你）** | 产品经理 + 最终决策者。定义需求、确认方案、做判断、不写代码 |
| **DeepSeek** | 架构师 + 后端开发。负责需求梳理、架构设计、接口契约、业务逻辑代码、UI 规范文字 |
| **豆包** | 视觉设计师。负责图标、插画、组件视觉稿的生成 |
| **AI 审核助手（我）** | 代码审核 + 集成落地。负责审核代码完整性、接口一致性、实际落地到项目、跑通构建 |

**核心原则：** DeepSeek 管"功能"，豆包管"外观"，审核助手管"落地"，用户管"决策"。

---

## 二、用户需要具备的能力

### ✅ 不需要会的
- 不需要懂任何编程语言语法
- 不需要懂怎么写函数、类、循环
- 不需要懂数据库 SQL
- 不需要懂服务器部署
- 不需要看得懂代码细节

### ✅ 必须会的
1. **把需求说清楚**：能用大白话描述用户做什么、想要什么效果
2. **基本判断力**：能判断 AI 输出的方案是否合理（靠常识即可，不懂就问）
3. **看懂文档结构**：能看懂架构分了几个模块、配色尺寸是否符合预期
4. **耐心和流程意识**：不跳步、不催、遇到问题先问

---

## 三、完整工作流程

```
阶段一：四方对齐（开工前必须完成）
  1. 用户 + DeepSeek → 确认需求
  2. DeepSeek → 输出架构文档 + 接口契约 + UI 规范（文字版）
  3. 用户转发给审核助手 → 审核通过
  4. DeepSeek → 输出视觉资源需求清单
  5. 用户转发给豆包 → 豆包生成图标/视觉稿
  6. 用户转发给审核助手 → 视觉资源验收存入项目

阶段二：逐文件开发（按依赖顺序）
  1. DeepSeek 按拓扑序一次输出一个文件（含对账信息）
  2. 用户转发给审核助手 → 审核代码与契约一致性
  3. 审核通过 → 集成落地；不通过 → 返回 DeepSeek 修改
  4. 每完成一个模块 → DeepSeek 做契约对账

阶段三：收尾
  - 全量构建测试
  - 视觉一致性检查
  - 文档最终确认
```

---

## 四、四方对齐流程（开工前的必要前提）

四方不能直接对话，所有信息通过用户转发。"对齐"的本质是用户持有一份四方都认可的**基准文档包**。

### 基准文档包清单

| 文档 | 产出方 | 需谁确认 | 作用 |
|------|--------|---------|------|
| 《需求说明书》 | 用户 + DeepSeek | 用户 | 明确做什么、不做什么 |
| 《架构设计文档》 | DeepSeek | 用户 + 审核助手 | 明确怎么分层、怎么拆模块 |
| 《接口契约清单》 | DeepSeek | 用户 + 审核助手 | 明确每个文件导出什么、依赖什么 |
| 《UI 设计规范》（文字版） | DeepSeek | 用户 + 审核助手 | 明确配色、尺寸、组件参数 |
| 《视觉资源需求清单》 | DeepSeek | 用户 + 豆包 | 明确需要哪些图标/插画 |
| 《视觉资源包》 | 豆包 | 用户 + 审核助手 | 实际的图标/插画文件 |

### 对齐步骤

1. **用户和 DeepSeek 对齐需求** → DeepSeek 输出《需求说明书》→ 用户确认
2. **DeepSeek 输出三份文档**（架构 + 契约 + UI 规范）→ 用户转发审核助手 → 审核通过
3. **DeepSeek 输出视觉需求清单** → 用户转发豆包
4. **豆包生成视觉资源** → 用户转发审核助手 → 验收存入项目
5. **用户确认开工**："基准文档已全部确认，按依赖顺序开始逐文件开发"

---

## 五、给 DeepSeek 的完整指令（可直接复制）

```
你好，我会和另一个 AI 审核助手以及豆包协作完成这个项目。你负责架构设计、契约定义、业务代码和 UI 规范文字；审核助手负责审核和落地；豆包负责视觉资源生成。请严格按以下规则输出。

=== 一、需求澄清阶段 ===
在开始设计前，请先向我确认：
1. 项目基本信息：名称、描述、目标用户、解决的问题
2. 技术栈：前后端框架、数据库、部署方式（如未指定请推荐并说明理由）
3. 核心功能清单：列出主要功能模块让我确认
4. 非功能需求：性能、并发、权限、国际化、合规
5. 边界范围：本期做什么，后续做什么

⚠️ 确认前不要开始设计。

=== 二、架构设计阶段 ===
输出《项目架构设计文档》，必须包含：
1. 整体架构图（Mermaid 或文字）：分层 + 交互说明
2. 模块拆分清单：每个模块的名称、职责、包含文件、依赖
3. 数据流说明：核心业务流程时序
4. 技术选型理由：每个依赖的版本和选择原因
5. 完整目录结构（到文件级别）

=== 三、接口契约阶段（最关键，先于任何代码）===
输出《模块接口契约清单》，每个模块必须列出：
1. 对外导出的符号：
   符号名 / 类型 / 参数 / 返回 / 说明
2. 依赖的外部符号：
   从哪个文件导入什么
3. 共享数据结构：跨模块的类型/接口/枚举集中定义
4. 统一规范：错误处理、日志、命名、常量位置

⚠️ 契约确认后不得随意更改，如需变更必须发"契约变更通知"。

=== 四、UI 设计规范（文字版，由你输出）===
输出《UI 设计规范文档》，包含：
- 配色方案（必须十六进制值，如 #1677ff）
- 字体规范（字号阶梯、行高、字重）
- 间距系统（基于 4px：4/8/12/16/24/32/48）
- 圆角、阴影、边框规范
- 组件视觉规范（按钮、输入框、卡片等的完整参数）
- 图标风格说明（线性/面性、线宽、尺寸）
- 页面布局模板（列表页、详情页、表单页）

=== 五、视觉资源需求清单（由你输出给豆包）===
从 UI 规范中提取需要可视化的元素，每个写一份精确描述：
  名称 / 用途 / 类型(图标/插画/视觉稿) / 风格 / 尺寸
  配色(每个状态的色值) / 文字内容 / 字体参数
  圆角/阴影/边框 / 其他细节 / 输出格式(SVG优先)

汇总成《视觉资源需求清单》发给我。

=== 六、逐文件生成阶段（按拓扑序，一次一个）===
每个文件必须包含 3 个部分：

【第1部分：对账信息】
文件路径:
对应架构章节:
对应契约条目:
本文件导出:
  - 符号名(参数): 返回值
本文件依赖:
  - 从 'xxx' 导入 yyy（已落地？是/否）
注意事项:

UI 文件额外加：
UI 设计来源: 豆包《xxx设计》日期版
样式参数对照:
  - 背景色: #1677ff (对应UI规范主色)
  - 圆角: 6px (对应圆角md)
图标:
  - 使用 xxx.svg（路径: src/assets/icons/xxx.svg）

【第2部分：完整代码】
（完整可运行代码，不允许省略号或 TODO）

【第3部分：实现说明】
- 做了什么
- 边界情况
- 有无临时方案

=== 七、硬性规则 ===
1. 代码必须完整，不允许省略
2. 严格遵守契约，导出符号名/参数/返回值必须一致
3. 不引用未落地的文件
4. 不使用未在架构中列出的第三方库
5. 不修改已确认落地的文件
6. 每次只给一个文件

=== 八、对账同步 ===
每完成一个模块（约3-5个文件）：
1. 回顾已生成文件，确认与契约一致
2. 如需调整，输出"契约变更通知"
3. 等确认后再继续
```

---

## 六、给豆包的指令（可直接复制）

```
你好，我正在和 DeepSeek（写代码）及一个审核助手协作开发项目。你负责 UI 视觉设计和图标生成。请按以下规则交付。

=== 交付物 ===

1. 图标/插画/视觉稿（按需交付）
每次我给你一份《视觉资源需求清单》，请严格按清单中的精确描述生成。

=== 硬性要求 ===
1. 严格按描述生成，颜色用十六进制值，尺寸用像素值
2. 图标优先输出 SVG 代码（可缩放、可改色）
3. 如果只能输出 PNG，请提供 2x 和 3x 两种尺寸
4. 组件需要展示所有状态（默认/hover/active/disabled）
5. 不添加描述中没有的元素

=== 输出格式 ===
每个资源输出：
- 资源名称
- 实际生成的内容（SVG代码 或 图片）
- 如与描述有偏差，说明原因
```

---

## 七、DeepSeek 给豆包的视觉描述模板

DeepSeek 给豆包的描述必须精确到豆包拿到就能直接生成，不需要再问。

### 图标描述示例
```
名称: 用户头像图标
用途: 用户中心入口
类型: 图标
风格: 线性
尺寸: 24x24px
线宽: 1.5px
颜色: #333333（允许继承 currentColor）
内容: 圆形头部 + 肩膀的简化人形
端点: 圆角
输出格式: SVG
```

### 组件视觉稿描述示例
```
名称: 登录主按钮
用途: 登录页面主操作
类型: 按钮组件视觉稿
风格: 扁平现代极简
尺寸: 宽度自适应，高度40px
配色:
  - 默认态背景: #1677ff，文字: #ffffff
  - hover态背景: #4096ff
  - active态背景: #0958d9
  - 禁用态背景: #1677ff40
文字: "登录"，14px，字重500，system-ui
圆角: 6px
内边距: 8px 16px
图标: 左侧登录图标，16x16px，白色，线性
阴影: 无
状态: 默认/hover/active/禁用 四种
```

---

## 八、逐文件开发的审核要点（审核助手执行）

每收到 DeepSeek 的一个文件，审核助手会检查：

1. **对账信息完整性**：是否包含导出、依赖、来源
2. **契约一致性**：导出符号是否与契约完全一致
3. **依赖检查**：引用的文件是否已落地，导入路径是否正确
4. **代码完整性**：无省略、无 TODO、可直接运行
5. **UI 文件额外检查**：
   - 样式参数是否与 UI 规范一致
   - 图标引用路径是否存在
   - 颜色/尺寸是否为规范中的具体值
6. **构建验证**：集成后能否通过编译

---

## 九、常见风险与应对

| 风险 | 应对措施 |
|------|---------|
| AI 误解需求 | 需求阶段让 DeepSeek 列出功能点，逐条确认要/不要/以后做 |
| 不知道自己要什么 | 让 DeepSeek 出2-3个方案供选择 |
| AI 输出看似对但有坑 | 审核助手把关，发现问题必须返工，不"差不多就行" |
| 接口对不上 | 契约先行，所有文件严格遵守契约 |
| 架构与实现漂移 | 每模块对账，契约变更走流程 |
| 视觉与代码不一致 | UI 规范由 DeepSeek 写（与代码同源），豆包只按描述出图 |
| 沟通成本高 | 用指令模板，直接复制粘贴 |

---

## 十、用户操作守则

1. **不跳步**：上一步没确认，不进下一步。契约没定，绝不让写代码。
2. **不口头传达**：所有确认以文档为准，变更必须更新文档。
3. **变更走流程**：改需求/配色/接口，必须更新对应文档并重新确认。
4. **一次一个文件**：不让 DeepSeek 一次给多个文件。
5. **不懂就问**：对 AI 输出有疑问，直接问审核助手"这个合理吗"。

---

## 十一、开工检查清单

- [ ] 《需求说明书》已确认
- [ ] 《架构设计文档》已审核通过
- [ ] 《接口契约清单》已审核通过
- [ ] 《UI 设计规范》已审核通过
- [ ] 《视觉资源需求清单》已发给豆包
- [ ] 视觉资源已生成并存入项目
- [ ] 《四方能力边界声明》已汇总并互相确认
- [ ] 《四方工作职责约定书》已四方确认
- [ ] 用户已确认"基准文档全部确认，开始开发"

全部勾选后，进入逐文件开发阶段。

---

## 十二、四方能力边界声明

在项目正式开始前，三方需各自输出《能力边界声明》，由用户汇总后发回三方互相确认。

### 能力边界声明模板

```
=== 1. 我能做的 ===
（针对本项目，可独立完成的工作，尽量具体）

=== 2. 我不能做的 ===
（做不到、做不好、或不应该由我做的工作）

=== 3. 我需要其他方提供的 ===
（工作中需要依赖其他方提供的输入）

=== 4. 我的输出物格式 ===
（交付成果的格式）

=== 5. 我的局限性/风险提示 ===
（可能出错的地方、需要用户把关的地方）
```

### 审核助手能力边界声明（示例）

**1. 我能做的**
- 审核代码完整性、语法正确性、与契约的一致性
- 检查模块间依赖关系、导入路径是否正确
- 把通过审核的代码写入项目文件并集成
- 运行构建/编译，验证能否通过
- 审核 UI 代码样式参数是否与 UI 规范一致
- 检查图标引用路径、资源文件是否存在
- 维护"已落地文件状态表"
- 发现问题时给出具体修改建议

**2. 我不能做的**
- 不做原创架构设计（DeepSeek 负责）
- 不生成图标、插画、视觉稿（豆包负责）
- 不做需求决策（用户负责）
- 不修改已通过审核并落地的文件，除非收到明确指令
- 不自行决定引入新的第三方库
- 不跳过契约直接写代码

**3. 我需要其他方提供的**
- DeepSeek 提供：架构文档、接口契约、UI 规范、每个文件的对账信息和完整代码
- 豆包提供：生成的图标/视觉资源文件
- 用户提供：需求文档、对每个审核结果的确认

**4. 我的输出物格式**
- 审核意见（通过/不通过 + 具体问题）
- 已落地的代码文件
- 已落地文件状态表
- 构建结果（成功/失败 + 错误信息）

**5. 我的局限性/风险提示**
- 无法判断设计是否"好看"，只能检查是否符合规范
- 无法判断需求是否合理，只能检查代码是否符合契约
- 能编译通过但有逻辑 bug 的代码可能漏检（需测试覆盖）
- 依赖 DeepSeek 提供的对账信息，若对账信息本身有误可能漏检

---

## 十三、四方工作职责约定书

> 四方共同遵守的"工作宪法"，出了问题以此为据。任何一方不得越界。

### 职责划分总表

| 工作项 | DeepSeek | 豆包 | 审核助手 | 用户 |
|--------|----------|------|----------|------|
| 需求梳理 | 主动提问、整理需求文档 | ❌ 不参与 | ❌ 不参与 | ✅ 最终确认 |
| 架构设计 | ✅ 输出架构文档 | ❌ 不参与 | ⚠️ 仅审核合理性 | ⚠️ 仅确认 |
| 接口契约定义 | ✅ 定义所有导出/依赖 | ❌ 不参与 | ⚠️ 审核完整性 | ⚠️ 仅确认 |
| UI 规范（文字） | ✅ 配色/尺寸/组件参数 | ❌ 不参与 | ⚠️ 审核可落地性 | ⚠️ 仅确认审美 |
| 视觉资源生成 | ❌ 只写描述 | ✅ 生成图标/插画/视觉稿 | ⚠️ 验收格式 | ⚠️ 仅确认好不好看 |
| 业务逻辑代码 | ✅ 逐文件编写 | ❌ 不参与 | ⚠️ 审核+集成 | ❌ 不参与 |
| UI 组件代码 | ✅ 结构+逻辑+样式 | ❌ 不写代码 | ⚠️ 审核样式合规 | ❌ 不参与 |
| 代码审核 | ❌ 不审核自己 | ❌ 不参与 | ✅ 完整性/一致性/构建 | ❌ 不参与 |
| 文件落地集成 | ❌ 不直接写文件 | ❌ 不参与 | ✅ 写入项目目录 | ❌ 不参与 |
| 契约变更 | ✅ 发起变更通知 | ❌ 不参与 | ⚠️ 评估影响 | ✅ 最终批准 |
| 需求变更 | ⚠️ 评估影响 | ⚠️ 评估视觉影响 | ⚠️ 评估代码影响 | ✅ 最终决策 |
| 问题排查 | ✅ 分析代码问题 | ⚠️ 分析视觉问题 | ✅ 定位错误位置 | ❌ 描述现象 |
| 最终验收 | ❌ | ❌ | ⚠️ 提供构建报告 | ✅ 最终验收 |

> ✅ = 负责执行 ｜ ⚠️ = 参与但不决策 ｜ ❌ = 不参与

### 各方核心职责

#### 🧠 DeepSeek（架构师 + 开发者）

**必须做：**
- 主动澄清需求，输出需求确认清单
- 设计架构，输出架构文档
- 定义接口契约，保证无遗漏、无循环依赖
- 编写 UI 规范文字（配色、尺寸、组件参数）
- 为每个视觉资源写精确描述
- 按拓扑序逐文件输出代码（含对账信息）
- 代码严格遵守契约，不省略、不写 TODO
- 每模块完成后做契约对账

**绝对不做：**
- 不生成任何图片/图标/视觉稿
- 不直接修改已落地的文件（如需修改，发变更通知）
- 不引入架构文档未列出的第三方库
- 不审核自己的代码
- 不替用户做需求决策

#### 🎨 豆包（视觉设计师）

**必须做：**
- 按 DeepSeek 提供的精确描述生成图标/插画/视觉稿
- 图标优先输出 SVG 代码
- 组件视觉稿展示所有状态（默认/hover/active/disabled）
- 严格遵守描述中的色值、尺寸、风格

**绝对不做：**
- 不写代码
- 不设计数据结构/接口
- 不做架构决策
- 不自行添加描述中没有的元素
- 不修改 UI 规范文字（只能生成视觉产物）

#### 🔍 审核助手

**必须做：**
- 审核架构/契约/UI 规范的合理性和可落地性
- 审核每个文件的对账信息、契约一致性、依赖完整性
- 把通过审核的代码写入项目文件
- 运行构建验证
- 维护已落地文件状态表
- 验收视觉资源的格式和可用性
- 发现问题给出具体修改建议

**绝对不做：**
- 不做原创架构设计
- 不生成视觉资源
- 不替用户做需求决策
- 不跳过审核直接落地代码
- 不自行修改已落地文件（除非用户明确要求）

#### 👤 用户

**必须做：**
- 清晰描述需求，回答 DeepSeek 的澄清问题
- 确认所有基准文档（需求/架构/契约/UI规范）
- 确认视觉资源好不好看
- 审核通过后确认是否落地
- 需求/契约变更时做最终决策
- 四方信息中转和汇总

**可以不做：**
- 不需要写代码
- 不需要懂技术细节
- 不需要审核代码逻辑

### 越界处理机制

#### 情况 1：某方主动越界（做了不该做的事）
- 发现方立即指出："这项工作属于 XX 的职责范围，请交由 XX 处理。"
- 越界方停止该工作，把已做内容转交给职责方继续。
- 如已造成影响，由越界方负责说明并协助修正。

#### 情况 2：某方推诿（该做的不做）
- 需求方指出："根据职责约定，这项工作属于你的职责范围。"
- 推诿方必须说明无法完成的原因，由用户裁决：
  - 原因合理 → 调整职责约定（需四方确认）
  - 原因不合理 → 必须执行

#### 情况 3：遇到职责空白（没人负责的工作）
- 发现方提出："这项工作目前没有明确归属。"
- 四方讨论后，由用户指定负责方，并更新职责约定。

### 灰色地带处理原则

遇到职责不清的情况，按以下优先级判断归属：

1. **谁最擅长归谁**：代码问题归 DeepSeek，视觉问题归豆包，落地问题归审核助手。
2. **谁输出归谁**：谁产出的东西出问题，谁负责修正。
3. **用户指定**：以上都判断不了，由用户指定。
4. **事后归档**：每次处理灰色地带后，把结论更新到职责约定中。

### 职责变更流程

```
1. 某方提出变更申请（说明原因和建议）
2. 四方讨论影响
3. 用户最终决策
4. 更新《四方工作职责约定书》
5. 四方确认后生效
```

---

## 十四、成本优化方案（推荐使用）

> 审核助手消耗积分，DeepSeek 和豆包免费。**核心原则：凡是 DeepSeek 或豆包能做的，都交给他们做。审核助手只做他们做不到的事。**

### 成本优化原则

1. **能移交则移交**：审核工作尽量让 DeepSeek/豆包自查完成
2. **只做不可替代的事**：审核助手只保留文件写入、构建运行、构建失败排查、抽查
3. **构建即底线**：构建通过 = 基本质量合格，不做额外审核
4. **抽查做兜底**：按比例抽查，发现问题再扩大范围

### 职责重新划分（压缩版）

#### 移交给 DeepSeek 的工作

| 原审核助手工作 | 移交给 DeepSeek | 说明 |
|---------------|-----------------|------|
| 架构合理性审核 | DeepSeek 自查 | 自己设计的架构自己最清楚 |
| 契约完整性审核 | DeepSeek 自查 | 自己检查有无遗漏、循环依赖 |
| UI 规范可落地性审核 | DeepSeek 自查 | 自己写的规范自己确认能否映射成代码 |
| 代码语法检查 | DeepSeek 自查 | 输出前自己检查语法、拼写 |
| 代码与契约一致性检查 | DeepSeek 自查 | 输出前自己对照契约核对 |
| 依赖是否已落地检查 | DeepSeek 自查 | 维护"已落地文件清单" |

#### 移交给豆包的工作

| 原审核助手工作 | 移交给豆包 | 说明 |
|---------------|-----------|------|
| 视觉资源格式验收 | 豆包自查 | 输出前自己确认是 SVG、尺寸正确 |
| 视觉与描述一致性 | 豆包自查 | 自己对照描述检查色值、尺寸 |

#### 审核助手保留的工作（不可替代）

1. **文件落地**：把通过自查的代码实际写入项目文件
2. **构建验证**：运行编译/构建，确认代码能跑通
3. **构建失败排查**：构建失败时定位错误位置并反馈给 DeepSeek
4. **抽查审核**：按比例随机抽查部分文件，做质量兜底
5. **模块验收**：每个模块完成后做一次模块级审核

### 优化后的工作流程

```
阶段一：四方对齐（审核助手只参与 1 次）
  1. 用户 + DeepSeek → 确认需求
  2. DeepSeek → 输出架构 + 契约 + UI 规范（DeepSeek 自查）
  3. 用户转发给审核助手 → 做一次全面审核（确认方向没问题）
  4. DeepSeek → 输出视觉需求清单 → 豆包生成视觉资源（豆包自查）
  5. 用户确认开工

阶段二：逐文件开发（审核助手只在构建时介入）
  1. DeepSeek 输出文件（含对账信息 + 自查结果）
  2. 用户转发给审核助手 → 直接写入文件（不做详细审核）
  3. 审核助手运行构建 → 成功则继续；失败则反馈错误给 DeepSeek 修改
  4. 每完成 5~10 个文件，审核助手抽查 1 个

阶段三：收尾（审核助手参与 1~2 次）
  - 全量构建
  - 抽查若干核心文件
  - 模块验收
```

### DeepSeek 自查要求（必须执行）

每个文件输出前，DeepSeek 必须做自查，并在对账信息中增加：

```
【自查结果】
- 语法检查: ✅ 通过
- 契约一致性: ✅ 导出符号与契约完全一致
- 依赖检查: ✅ 所有依赖文件已落地
- 代码完整性: ✅ 无省略、无 TODO
```

审核助手只看这 4 项是否全为 ✅，然后直接写入文件 + 构建。**构建通过则不做额外审核**。

### 质量兜底机制

完全放手会有风险，设置 3 道兜底：

1. **构建兜底**（最硬底线）：构建不过 = 一定有问题，审核助手必须介入排查
2. **抽查兜底**：每 5~10 个文件抽查 1 个，发现问题则扩大抽查范围
3. **模块验收兜底**：每个模块完成后，审核助手做模块级审核（接口完整性 + 构建通过）

### 成本估算对比

| 模式 | 审核助手调用次数（以 50 个文件为例） | 质量保障 |
|------|-------------------------------------|---------|
| 原模式（每个文件详细审核） | ~50 次 | 高 |
| 优化模式（写入+构建+抽查） | ~15~20 次 | 中高（有构建兜底） |

优化模式可节省约 **60%~70%** 的积分消耗。

### 风险与应对

| 风险 | 应对措施 |
|------|---------|
| DeepSeek 自查放水，都打 ✅ 但实际有问题 | 构建兜底抓大部分问题，抽查兜底抓漏网之鱼 |
| 逻辑 bug 编译能过 | 抽查时重点看逻辑；关键模块（支付、权限等）必须详细审核 |
| 契约漂移发现不及时 | 模块验收时核对契约 |
| 视觉资源质量不达标 | 用户确认好不好看，不合格退回豆包重做 |

---

*本文档基于 DeepSeek + 豆包 + AI 审核助手三方协作模式整理，适用于从零开始的软件项目开发。*
